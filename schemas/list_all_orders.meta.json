{
  "description": "List recent orders across ALL customers, no filtering. Use when the user asks for 'all orders', 'recent orders', or any aggregate over orders without a specific customer. Optional 'limit' (1-100, default 25).",
  "requires_permissions": ["orders:read"],
  "write": false
}
